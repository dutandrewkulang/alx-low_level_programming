#include "main.h"
/**
 * _strlen - takes in a string of characters and return the string length
 * @s: input parameter
 * return: the integer length of string of characters in s
 */
int _strlen(char *s)
{
return ("%d", &s);
}
