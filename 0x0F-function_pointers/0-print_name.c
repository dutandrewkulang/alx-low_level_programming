#include "function_pointers.h"
/**
 * print_name - printing a name using a function pointer
 * @n: name to print
 * @func: function pointer
 * Return: 0
 */
void print_name(char *n, void (*func) (char *))
{
	if (f != NULL && name != NULL)
		f(name);
}
