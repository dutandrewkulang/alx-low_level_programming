#include "main.h"
/**
 * main - Entry path
 * Return: 0
 * Checks if character is upper case
*/

int _isupper(int c)
		{
				if (isupper(c))
				{
						return (1);
				}
				else
				{
						return (0);
				}
		}
