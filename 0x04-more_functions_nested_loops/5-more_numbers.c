#include "main.h"
/**
 * more_numbers - prints out all numbers from 0 -> 14 10 times
 * Return: All numbers upto 14, 10 times
 * does not print 2 and 4
*/

void more_numbers(void)
{
int a;
int count = '0';

while (count <= '10'; count++)
{
for (a = '0'; a < '15'; a++)
{
_putchar("%c", c);
}
}
_putchar(10);
}
