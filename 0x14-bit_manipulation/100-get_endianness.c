#include "main.h"

/**
 * get_endianness - checks for endianness of processor
 * Return: 0 on success
 */

int get_endianness(void){
    unsigned int m = 1, *ptr;
    ptr = &m;
    return (*((char *) p));
}
